# 변환·로컬 검증용 전체 의존성 (torch 계열 포함).
# 배포 이미지 런타임은 requirements-runtime.txt 만 설치한다.
--extra-index-url https://download.pytorch.org/whl/cpu

torch>=2.9.0
timm>=1.0.28
safetensors>=0.8.0
fastapi>=0.141.1
uvicorn>=0.52.2
python-multipart>=0.0.32
pillow>=11.3.0
numpy>=2.2.6
huggingface_hub>=1.27.0
mangum>=0.19.0

# export_onnx.py — onnx 는 변환, onnxruntime 은 변환 직후 출력 대조에 쓴다.
onnx>=1.22.0
onnxruntime>=1.29.0
